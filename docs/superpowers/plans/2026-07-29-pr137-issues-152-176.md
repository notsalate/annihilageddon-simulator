# PR #137: план задач #152–#176

## Цель

Закрыть находки повторного ревью по задачам #152–#176 в draft PR #137 и зафиксировать только проверенные результаты.

## Границы

- Не менять package metadata, lockfile, зависимости, CI/CD и `data/import/**`.
- Сохранять deterministic seeded RNG, stable IDs и draft-статус PR.
- Этот завершающий документационный коммит не меняет проверенный снимок кода и не создаёт самоссылочный SHA.

## Итоговый снимок кода

Проверенный удалённый снимок кода — `c7e2e74be809493ead81860f1d6e1813546ef4be`. На нём успешно завершились:

- `npm run check`;
- `npm run report:card-runtime-clusters`: total 134, fullRuntime 43, missingRuntime 91, clustered 134, needsClusterDecision 0;
- обе проверки `git diff --check`;
- финальные Spec и Standards ревью точного диапазона от `62777c3` до `c7e2e74`;
- GitHub secrets/SAST/OSV/CodeQL.

PR body синхронизирован с проверенным снимком кода `c7e2e74` и текущим
документационным HEAD; PR #137 остаётся draft.

## Контракты и доказательства задач

| Задача | Выполненный результат                                                                                                                                        | Владение и точечное доказательство                                                                                                                                       | Проверка                                                                                                                                                             |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #152   | Strategy-facing choices изолированы; события используют каноническую Defense.                                                                                | `src/engine/attack-defense.ts`; `tests/defense-choice.test.ts`.                                                                                                          | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #153   | Ordinary и redirect-защита соблюдают payment → movement → branch и rollback.                                                                                 | `src/engine/attack-defense.ts`; `tests/attack-resolution.test.ts`.                                                                                                       | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #154   | Source matrix Catalog задаёт точные source kinds, включая `deadWizardToken`.                                                                                 | `src/engine/effect-runtime-registry.ts`, `src/engine/effective-values.ts`; `tests/setup-effects.test.ts`, `tests/effect-runtime-applicability.test.ts`.                  | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #155   | Catalog operation владеет policy и decode своего effect family.                                                                                              | `src/engine/effect-runtime-registry.ts`, `src/engine/runtime-effect-decoder.ts`; `tests/effect-runtime-catalog-errors.test.ts`.                                          | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #156   | Типизированные Catalog operations не раскрывают raw payload consumer-ам.                                                                                     | `src/engine/effect-runtime-registry.ts`; `tests/effect-runtime-catalog-types.test.ts`.                                                                                   | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #157   | Валидация effect payload остаётся на decoder boundary.                                                                                                       | `src/engine/runtime-effect-decoder.ts`, `src/engine/effect-runtime-registry.ts`; `tests/validation.test.ts`.                                                             | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #158   | Runtime applicability проходит зарегистрированную policy Catalog.                                                                                            | `src/engine/effect-runtime-registry.ts`; `tests/effect-runtime-applicability.test.ts`.                                                                                   | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #159   | Ошибки Catalog возвращаются через типизированный result без неявного fallback.                                                                               | `src/engine/effect-runtime-registry.ts`; `tests/effect-runtime-catalog-errors.test.ts`.                                                                                  | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #160   | Данные setup-effects вызывают зарегистрированные Catalog operations.                                                                                         | `src/engine/data.ts`, `src/engine/effect-runtime-registry.ts`; `tests/setup-effects.test.ts`.                                                                            | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #161   | Каждая Catalog entry имеет явную policy source kinds; decoder/Catalog guards закрывают публичные production entrypoints и статические цепочки export/import. | `src/engine/effect-runtime-registry.ts`, `scripts/check-engine-typed-access.mjs`; `tests/effect-runtime-catalog-types.test.ts`, `tests/public-entrypoint-guard.test.ts`. | `npm run check:engine-typed-access`; `npm test`; `npm run check` на `c7e2e74`.                                                                                       |
| #162   | Effective values получает decoded modifier, не читая raw fields.                                                                                             | `src/engine/effective-values.ts`, `src/engine/effect-runtime-registry.ts`; `tests/effective-values.test.ts`.                                                             | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #163   | Resurrection декодируется Catalog до проверки применимости и не скрывает ошибку fallback-значением.                                                          | `src/engine/effect-runtime.ts`, `src/engine/effect-runtime-registry.ts`; `tests/runtime-regression.test.ts`.                                                             | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #164   | Attack replacements, включая Wand, используют Catalog-owned profile path.                                                                                    | `src/engine/effect-runtime.ts`, `src/engine/effect-runtime-registry.ts`; `tests/attack-replacement-ongoing.test.ts`.                                                     | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #165   | Status и damage attacks используют общий player-controlled profile.                                                                                          | `src/engine/effect-runtime.ts`, `src/engine/attack-resolution.ts`; `tests/action-loop.test.ts`.                                                                          | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #166   | `attack_damage` строит и проверяет атомарный payment plan до мутаций.                                                                                        | `src/engine/runtime-effect-decoder.ts`, `src/engine/effect-runtime-registry.ts`; `tests/action-loop.test.ts`.                                                            | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #167   | `afterDamageDealt` возвращает error и `gameEnd` через damage boundary.                                                                                       | `src/engine/effect-runtime.ts`, `src/engine/trigger-dispatch.ts`; `tests/trigger-dispatch-ongoing.test.ts`, `tests/trigger-dispatch-errors.test.ts`.                     | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #168   | `topdeck_gained_card` выбирает apply/decline до перемещения карты.                                                                                           | `src/engine/effect-runtime.ts`; `tests/action-loop.test.ts`.                                                                                                             | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #169   | Fork клонирует карточные зоны через Ledger descriptors; metadata защищены от мутаций и повторного factory call.                                              | `src/engine/control-ledger.ts`, `src/engine/game-state-fork.ts`; `tests/game-state-fork.test.ts`, `tests/control-ledger-zones.test.ts`.                                  | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #170   | Ledger traversal сохраняет scoring eligibility и симуляционный VP `5:4`.                                                                                     | `src/engine/control-ledger.ts`, `src/engine/effective-values.ts`; `tests/effective-values.test.ts`, `tests/simulation.test.ts`.                                          | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #171   | Resolved card удаляется через Ledger, missing-card остаётся явной ошибкой.                                                                                   | `src/engine/effect-runtime.ts`, `src/engine/control-ledger.ts`; `tests/action-loop.test.ts`.                                                                             | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #172   | Owner invariants обходят descriptors, inventory guard не опирается на consumer zone list.                                                                    | `src/engine/control-ledger.ts`, `src/engine/invariants.ts`, `scripts/check-engine-typed-access.mjs`; `tests/control-ledger.test.ts`, `tests/engine-guards.test.ts`.      | `npm run check:engine-typed-access`; `npm test`; `npm run check` на `c7e2e74`.                                                                                       |
| #173   | AST и поведенческие guards закрывают именованные, default, star и namespace-цепочки, alias и циклы; trusted adapters перечислены явно.                       | `scripts/check-engine-typed-access.mjs`, `scripts/lib/check-protected-public-entrypoints.mjs`; `tests/public-entrypoint-guard.test.ts`, `tests/engine-guards.test.ts`.   | `npm run check:engine-typed-access`; `npm test`; `npm run check` на `c7e2e74`.                                                                                       |
| #174   | Ledger ownership/inventory tests находятся рядом с владельцем поведения.                                                                                     | `tests/control-ledger.test.ts`, `tests/control-ledger-zones.test.ts`, `tests/run-tests.ts`.                                                                              | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #175   | Terminal Market Flow использует узкий fixture helper со stable IDs.                                                                                          | `tests/helpers/market-flow-fixtures.ts`, `tests/market-flow-terminal.test.ts`.                                                                                           | `npm test`; `npm run check` на `c7e2e74`.                                                                                                                            |
| #176   | Доказательства и checklist синхронизированы с проверенным удалённым снимком кода `c7e2e74`; PR body обновлён, PR остаётся draft.                             | `docs/superpowers/specs/2026-07-29-pr137-issues-152-176-design.md`, этот plan, PR #137 body.                                                                             | `npm run check`; `npm run report:card-runtime-clusters`; обе `git diff --check`; финальные Spec и Standards ревью точного диапазона; GitHub secrets/SAST/OSV/CodeQL. |

Вертикальный срез `PlayerDecisionView` из `.scratch/pr137-review-fixes/issues/01-isolate-effect-choice-player-state.md` входит в проверенный снимок кода: глубокие readonly-снимки изолируют `request.player` и кандидатов выбора игрока или карты, сохраняя выбор по устойчивой identity, порядок кандидатов и события выбора. Исходные критерии #152 не менялись.

## Критерии завершения #176

- [x] Design и plan отражают проверенный удалённый снимок кода `c7e2e74`, а не локальный технический снимок.
- [x] На `c7e2e74` выполнены `npm run check`, отчёт кластеров и обе `git diff --check`.
- [x] Финальные Spec и Standards ревью точного диапазона `62777c3...c7e2e74` прошли без замечаний.
- [x] #161/#173 закрывают production entrypoints, именованные, default, star и namespace-цепочки, alias и циклы; trusted adapters перечислены явно.
- [x] GitHub secrets/SAST/OSV/CodeQL зелёные; PR body синхронизирован с `c7e2e74` и текущим документационным HEAD; PR #137 остаётся draft.
- [x] Этот завершающий документационный коммит документирует проверенный снимок кода и не меняет код.
