# PR #137: план задач #152–#176

## Цель

Исправить все находки повторного ревью по draft PR #137 и синхронизировать #176 только с проверенными результатами.

## Границы

- Не менять package metadata, lockfile, зависимости, CI/CD и `data/import/**`.
- Сохранять deterministic seeded RNG, stable IDs и draft-статус PR.
- Изолировать независимые изменения в worktree, проверять и коммитить их отдельно; интеграционная ветка получает только проверенные коммиты.

## Технический статус

| Задачи           | Состояние                             | Подтверждённый результат                                                                                                                |
| ---------------- | ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| #152, #153       | выполнено в интеграционной ветке      | Канонический immutable snapshot Defense и единая транзакция защиты; события отражают канонический выбор.                                |
| #154, #162, #163 | выполнено в интеграционной ветке      | Точная source matrix с `deadWizardToken`; Catalog декодирует effect до проверки timing и применимости.                                  |
| #164–#168        | выполнено в интеграционной ветке      | Один путь replacement через Catalog, общий attack profile, атомарная оплата, propagation `afterDamageDealt`, optional topdeck choice.   |
| #169–#172        | выполнено в интеграционной ветке      | Descriptor-driven cloning, прежняя scoring eligibility `5:4`, Ledger removal/invariants и полный structural guard.                      |
| #173             | выполнено в интеграционной ветке      | Восстановлено устойчивое архитектурное coverage Catalog и inventory seams.                                                              |
| #174–#175        | выполнено в интеграционной ветке      | Ledger-тесты и Market Flow fixtures остаются в владельцах поведения.                                                                    |
| #176             | в процессе интеграционного завершения | Документация синхронизирована с архитектурой; финальные SHA, команды, отчёт кластеров, GitHub checks и PR body ожидают root-интеграцию. |

## Порядок закрытия

1. Интегрировать проверенные технические коммиты и разрешить конфликты с сохранением всех ownership boundaries.
2. Выполнить финальные проверки на интеграционной ветке: `npm run check`, `npm run report:card-runtime-clusters`, `git diff --check` и review фактического диапазона.
3. Убедиться, что документация описывает только фактическую архитектуру и выполненные проверки. При изменении контрактов пройти DOX chain.
4. Обновить body PR #137 фактическими результатами, closing references для завершённых задач и сохранить draft.
5. Запушить ветку, дождаться GitHub checks и повторно убедиться, что PR не переведён в ready/merge.

## Критерии завершения #176

- [x] Design и plan не объявляют устаревшие implementation details или непроведённые проверки завершёнными.
- [x] В документации отражены canonical defense/events, source matrix и raw decode boundary, Catalog attack replacement, propagation `afterDamageDealt`, Ledger descriptors/scoring и AST guards #173.
- [ ] Интеграционная ветка прошла `npm run check`.
- [ ] Выполнен `npm run report:card-runtime-clusters` и записан фактический результат.
- [ ] Выполнены `git diff --check` и финальное двухосевое ревью полного диапазона.
- [ ] Draft PR обновлён фактическим head, проверками и closing references.
- [ ] GitHub security, SAST, supply-chain и CodeQL зелёные; draft-статус подтверждён.
