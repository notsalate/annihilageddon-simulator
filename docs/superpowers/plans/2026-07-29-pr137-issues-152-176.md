# PR #137: план задач #152–#176

## Цель

Исправить все находки повторного ревью по draft PR #137 и синхронизировать #176 только с проверенными результатами.

## Границы

- Не менять package metadata, lockfile, зависимости, CI/CD и `data/import/**`.
- Сохранять deterministic seeded RNG, stable IDs и draft-статус PR.
- Изолировать независимые изменения в worktree, проверять и коммитить их отдельно; интеграционная ветка получает только проверенные коммиты.

## Контракты и доказательства задач

Статус «выполнено» означает: изменение уже входит в текущую интеграционную историю, а его владелец и точечная suite перечислены ниже. Он не заменяет финальную проверку всего диапазона. Предыдущий подтверждённый remote-head — `34729637db30b2321f07549c05751c08cdf21f88`; на нём прошли полный `npm run check`, отчёт кластеров, финальные Spec/Standards review и GitHub security/SAST/OSV/CodeQL. Локальный технический head `4b5d8f83c9969874e8b9301225851f041eda5d30` не отправлен в remote и не является финальным head PR: на нём свежие `npm run check`, отчёт кластеров и обе проверки `git diff --check` завершились с exit 0, но GitHub checks и PR body для него не обновлялись.

| Задача | Статус и результат                                                                                                                                                                                                                                                       | Владение и точечное доказательство                                                                                                                                  | Проверка                                                                                                                                                                                                                        |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #152   | Выполнено: strategy-facing choices изолированы, события используют каноническую Defense.                                                                                                                                                                                 | `src/engine/attack-defense.ts`; `tests/defense-choice.test.ts` покрывает forged/mutable choice и события.                                                           | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #153   | Выполнено: ordinary и redirect-защита соблюдают payment → movement → branch и rollback.                                                                                                                                                                                  | `src/engine/attack-defense.ts`; `tests/attack-resolution.test.ts` покрывает обе ветви.                                                                              | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #154   | Выполнено: source matrix Catalog задаёт точные source kinds, включая `deadWizardToken`.                                                                                                                                                                                  | `src/engine/effect-runtime-registry.ts`, `src/engine/effective-values.ts`; `tests/setup-effects.test.ts` и `tests/effect-runtime-applicability.test.ts`.            | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #155   | Выполнено: Catalog operation остаётся владельцем policy и decode для своего effect family.                                                                                                                                                                               | `src/engine/effect-runtime-registry.ts`, `src/engine/runtime-effect-decoder.ts`; `tests/effect-runtime-catalog-errors.test.ts`.                                     | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #156   | Выполнено: типизированные Catalog operations не раскрывают raw payload consumer-ам.                                                                                                                                                                                      | `src/engine/effect-runtime-registry.ts`; `tests/effect-runtime-catalog-types.test.ts`.                                                                              | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #157   | Выполнено: валидация effect payload остаётся на decoder boundary.                                                                                                                                                                                                        | `src/engine/runtime-effect-decoder.ts`, `src/engine/effect-runtime-registry.ts`; `tests/validation.test.ts`.                                                        | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #158   | Выполнено: runtime applicability проходит зарегистрированную policy Catalog.                                                                                                                                                                                             | `src/engine/effect-runtime-registry.ts`; `tests/effect-runtime-applicability.test.ts`.                                                                              | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #159   | Выполнено: ошибки Catalog возвращаются через типизированный result без неявного fallback.                                                                                                                                                                                | `src/engine/effect-runtime-registry.ts`; `tests/effect-runtime-catalog-errors.test.ts`.                                                                             | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #160   | Выполнено: данные setup-effects вызывают зарегистрированные Catalog operations.                                                                                                                                                                                          | `src/engine/data.ts`, `src/engine/effect-runtime-registry.ts`; `tests/setup-effects.test.ts`.                                                                       | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #161   | Выполнено: каждая Catalog entry имеет явную policy source kinds, fallback удалён; закрытая decoder/Catalog поверхность отслеживает транзитный export, прямой re-export, alias и default export.                                                                          | `src/engine/effect-runtime-registry.ts`, `scripts/check-engine-typed-access.mjs`; `tests/effect-runtime-catalog-types.test.ts`, `tests/engine-guards.test.ts`.      | `npm run check:engine-typed-access`; `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                  |
| #162   | Выполнено: effective values получает decoded modifier, не читая raw fields.                                                                                                                                                                                              | `src/engine/effective-values.ts`, `src/engine/effect-runtime-registry.ts`; `tests/effective-values.test.ts`.                                                        | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #163   | Выполнено: resurrection декодируется Catalog до проверки применимости и не скрывает ошибку fallback-значением.                                                                                                                                                           | `src/engine/effect-runtime.ts`, `src/engine/effect-runtime-registry.ts`; `tests/runtime-regression.test.ts`.                                                        | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #164   | Выполнено: attack replacements, включая Wand, используют только Catalog-owned profile path.                                                                                                                                                                              | `src/engine/effect-runtime.ts`, `src/engine/effect-runtime-registry.ts`; `tests/attack-replacement-ongoing.test.ts`.                                                | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #165   | Выполнено: status и damage attacks используют общий player-controlled profile.                                                                                                                                                                                           | `src/engine/effect-runtime.ts`, `src/engine/attack-resolution.ts`; `tests/action-loop.test.ts`.                                                                     | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #166   | Выполнено: `attack_damage` строит и проверяет атомарный payment plan до мутаций.                                                                                                                                                                                         | `src/engine/runtime-effect-decoder.ts`, `src/engine/effect-runtime-registry.ts`; `tests/action-loop.test.ts`.                                                       | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #167   | Выполнено: `afterDamageDealt` возвращает error и `gameEnd` через damage boundary.                                                                                                                                                                                        | `src/engine/effect-runtime.ts`, `src/engine/trigger-dispatch.ts`; `tests/trigger-dispatch-ongoing.test.ts`, `tests/trigger-dispatch-errors.test.ts`.                | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #168   | Выполнено: `topdeck_gained_card` выбирает apply/decline до перемещения карты.                                                                                                                                                                                            | `src/engine/effect-runtime.ts`; `tests/action-loop.test.ts`.                                                                                                        | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #169   | Выполнено: fork клонирует карточные зоны через Ledger descriptors, metadata защищены от мутаций и повторного factory call.                                                                                                                                               | `src/engine/control-ledger.ts`, `src/engine/game-state-fork.ts`; `tests/game-state-fork.test.ts`, `tests/control-ledger-zones.test.ts`.                             | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #170   | Выполнено: Ledger traversal сохраняет scoring eligibility и симуляционный VP `5:4`.                                                                                                                                                                                      | `src/engine/control-ledger.ts`, `src/engine/effective-values.ts`; `tests/effective-values.test.ts`, `tests/simulation.test.ts`.                                     | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #171   | Выполнено: resolved card удаляется через Ledger и missing-card остаётся явной ошибкой.                                                                                                                                                                                   | `src/engine/effect-runtime.ts`, `src/engine/control-ledger.ts`; `tests/action-loop.test.ts`.                                                                        | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #172   | Выполнено: owner invariants обходят descriptors, а inventory guard не опирается на consumer zone list.                                                                                                                                                                   | `src/engine/control-ledger.ts`, `src/engine/invariants.ts`, `scripts/check-engine-typed-access.mjs`; `tests/control-ledger.test.ts`, `tests/engine-guards.test.ts`. | `npm run check:engine-typed-access`; `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном 4b5d8f8.                                                                                    |
| #173   | Выполнено: AST и поведенческие проверки закрывают decoder/Catalog re-export обходы через транзитный export, прямой re-export, alias и default export; междоменная `review-final-lint` удалена.                                                                           | `scripts/check-engine-typed-access.mjs`; `tests/effect-runtime-catalog-types.test.ts`, `tests/engine-guards.test.ts`.                                               | `npm run check:engine-typed-access`; `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                  |
| #174   | Выполнено: Ledger ownership/inventory tests находятся рядом с владельцем поведения.                                                                                                                                                                                      | `tests/control-ledger.test.ts`, `tests/control-ledger-zones.test.ts`, `tests/run-tests.ts`.                                                                         | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #175   | Выполнено: terminal Market Flow использует узкий fixture helper со stable IDs.                                                                                                                                                                                           | `tests/helpers/market-flow-fixtures.ts`, `tests/market-flow-terminal.test.ts`.                                                                                      | `npm test`; полный `npm run check` подтверждён на remote-head `3472963`, свежий — на локальном `4b5d8f8`.                                                                                                                       |
| #176   | В процессе: старое remote evidence подтверждено на `3472963`; локальные исправления, включая вертикальный срез `PlayerDecisionView` из `.scratch/pr137-review-fixes/issues/01-isolate-effect-choice-player-state.md`, проверены на `4b5d8f8`, но не отправлены в remote. | `docs/superpowers/specs/2026-07-29-pr137-issues-152-176-design.md`, этот plan, PR #137 body.                                                                        | На `3472963` завершены полный `npm run check`, отчёт кластеров, Spec/Standards review и GitHub checks; на `4b5d8f8` завершены `npm run check`, отчёт кластеров и обе `git diff --check`; обновление PR/GitHub pending отправки. |

## Порядок закрытия

1. Сохранить проверенные локальные исправления `4b5d8f8` и их отдельные Spec/Standards review.
2. Убедиться, что документация различает подтверждённый remote-head `3472963` и непереданный локальный `4b5d8f8`; при изменении контрактов пройти DOX chain.
3. После отправки локального head обновить body PR #137 фактическим удалённым head, проверками и closing references для завершённых задач; сохранить draft.
4. Дождаться GitHub checks и повторно убедиться, что PR не переведён в ready/merge.

## Критерии завершения #176

- [x] Design и plan не объявляют устаревшие implementation details или непроведённые проверки завершёнными.
- [x] В документации отражены canonical defense/events, source matrix и raw decode boundary, Catalog attack replacement, propagation `afterDamageDealt`, Ledger descriptors/scoring и AST guards #173.
- [x] На подтверждённом remote-head `3472963` выполнены `npm run check`, отчёт кластеров, финальное двухосевое review и GitHub security/SAST/OSV/CodeQL; PR остался draft.
- [x] На локальном `4b5d8f8` выполнены `npm run check`, `npm run report:card-runtime-clusters` (total 134, fullRuntime 43, missingRuntime 91, clustered 134, needsClusterDecision 0) и обе `git diff --check`.
- [x] #161/#173 синхронизированы: guards отслеживают транзитный export, прямой re-export, alias и default export; `review-final-lint` удалена.
- [x] Вертикальный срез `PlayerDecisionView` из `.scratch/pr137-review-fixes/issues/01-isolate-effect-choice-player-state.md` реализован на `4b5d8f8`: глубокие readonly-снимки изолируют `request.player` и кандидатов выбора игрока или карты, не меняя исходные критерии #152.
- [ ] Локальный `4b5d8f8` отправлен в remote, а PR body обновлён фактическим удалённым head, проверками и closing references.
- [ ] Для отправленного локального head подтверждены GitHub security/SAST/OSV/CodeQL и draft-статус PR.
