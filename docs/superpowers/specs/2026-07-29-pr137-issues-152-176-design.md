# PR #137: задачи #152–#176 — дизайн реализации

## Цель

Закрыть все незавершённые требования задач #152–#176 в draft PR #137, не меняя правила игры за пределами критериев приёмки.

## Проверенный статус интеграции

- #154–#160 уже входят в исходный head `9c89ab8` (`refactor(effects): закрыть Catalog operations`).
- В интеграционной ветке выполнены: #152 — `873483b`, #153 — `2da8e5e`, #161 — `883e503`, #162 — `ed52186`, #163 — `c239966`, #164 — `dea4044`, #165 — `36214ff`, #166 — `c682bd3`, #167 — `add9884`, #168 — `6a6bb8f`, #169 — `4d5cdc1`, #170 — `d3f9d0f`, #171 — `80d993d`, #172 — `e088ef6`, #173 — `4a7ed05`, #174 — `2ccbe86`, #175 — `0ec97ad`.
- До документационного коммита итоговый head — `80d993d`; рабочее дерево чистое.
- #176 локально завершена: она зафиксировала проверенные команды и подготовила текст draft PR. Удалённые шаги остаются незавершёнными: после интеграции нужно обновить draft PR с final HEAD и `Closes #152` … `Closes #176`, затем подтвердить зелёные GitHub checks security, SAST, supply-chain и CodeQL. Draft-статус PR сохраняется; merge, ready и auto-merge не включаются.

## Архитектура

1. Defense получает каноническое сопоставление `choiceId → legal Defense` до передачи options стратегии. Выбранный объект и его поля не являются источником истины. Обычная и redirect-защита проходят одну транзакционную последовательность: payment → movement → branch.
2. Effect Runtime Catalog остаётся единственной точкой decode, source-kind и runtime-mode policy. Consumers для effective values, resurrection и attack replacements вызывают узкие Catalog operations вместо чтения raw effect payload.
3. Player-controlled attacks используют общий attack profile и атомарный cost plan. `afterDamageDealt` и optional `topdeck_gained_card` проходят typed dispatch и choice boundary.
4. Control Ledger владеет physical card inventory для fork, scoring, lookup/removal и owner invariants. Consumers не перечисляют зоны вручную.
5. Регрессии живут рядом с владельцем поведения; финальный документ PR отражает только фактически выполненные проверки и остаётся draft.

## Исполнение

- Каждый issue получает одного `coder` и отдельный worktree, созданный от текущего интеграционного head.
- Задачи выполняются последовательно и коммитятся из worktree агента. Интеграционная ветка получает только проверенные коммиты.
- Каждое изменение начинает TDD-циклом: новый тест должен сначала упасть по ожидаемой причине.
- Общий объём worktree не должен превышать доступное место; зависимости устанавливаются только там, где это требуется для проверки.

## Ограничения

- PR #137 остаётся draft: без merge, ready и auto-merge.
- Не менять package metadata, lockfile, зависимости, CI/CD и импортные данные.
- Сохранять seeded RNG, stable IDs, разделение runtime и `data/import/**`.
- Не включать в PR не связанные с issues изменения.

## Проверка

- Исторические RED/GREEN-выводы отдельных задач не дублируются в этом документе: подтверждены только их интеграционные коммиты.
- Для #176 выполнены `npm run check` и `npm run report:card-runtime-clusters`: проверка завершилась успешно; отчёт содержит 134 карты, 43 `fullRuntime`, 91 `missingRuntime`, 134 clustered и 0 `needsClusterDecision`. `git diff --check` выполняется перед коммитом и после него.
- Подготовлен текст draft PR с `Closes #152` … `Closes #176`; его обновление и проверка GitHub checks остаются удалёнными шагами после интеграции. Удалённый PR этой задачей не меняется.
