# PR #137: задачи #152–#176 — дизайн реализации

## Цель

Закрыть находки повторного ревью по задачам #152–#176 в draft PR #137, не меняя правила игры вне критериев приёмки.

## Проверенный статус интеграции

Проверенный удалённый снимок кода PR — `6a2ddb6f612903e1625c8174d238578e11473688`. На нём успешно завершились `npm run check`, отчёт кластеров (total 134, fullRuntime 43, missingRuntime 91, clustered 134, needsClusterDecision 0), обе проверки `git diff --check`, финальное Standards review и GitHub secrets/SAST/OSV/CodeQL. Единственная финальная находка Spec по `NamespaceImport` исправлена в `6a2ddb6`, после чего точечное повторное Spec/Standards ревью завершилось без находок. PR body обновлён для этого head; PR остался draft.

- #152: стратегия получает неизменяемый снимок легальных вариантов защиты; исполнение и события используют каноническую Defense, определённую до вызова стратегии.
- #154, #162 и #163: матрица источников Catalog различает `card`, `wizardProperty` и `deadWizardToken`; raw effects сначала декодируются Catalog, затем проверяются timing и применимость.
- #164: замены атак проходят единственный Catalog-owned путь, включая Wand и ongoing sources.
- #167: результат `afterDamageDealt`, включая ошибку и `gameEnd`, возвращается через границу урона.
- #169: клонирование карточных зон строится по descriptors; metadata descriptor-зон неизменяемы и защищены от повторного вызова, подмены имени и общего состояния fork.
- #170: подсчёт VP обходит Ledger, но сохраняет прежнюю eligibility для scoring и результат `5:4`.
- #161 и #173: закрытая поверхность decoder/Catalog отслеживает транзитный export, прямой re-export, alias, default export и `NamespaceImport`; междоменная проверка `review-final-lint` удалена. Структурные AST-проверки контролируют полный inventory seam и архитектурные границы Catalog; coverage не зависит от параллельного перечня зон в consumer.
- #174 и #175 остаются выполненными без изменения своих границ.

## Архитектура

1. Defense определяется каноническим snapshot до передачи choices стратегии. Выбор стратегии — только идентификатор; mutable fields переданного объекта не участвуют ни в исполнении, ни в журналировании. Ordinary и redirect-защита соблюдают порядок payment → movement → branch.
2. Effect Runtime Catalog — единственная граница decode, source-kind и policy. `deadWizardToken` передаётся настоящим source context; consumers не читают raw `timing`, `effectId` или payload до Catalog.
3. Attack replacement и `afterDamageDealt` проходят типизированные Catalog/dispatch boundaries, поэтому ошибки и terminal result не теряются.
4. Control Ledger описывает physical card zones декларативно. Его descriptors используются для cloning, inventory guards и допустимого обхода карт для scoring, не расширяя eligibility игрока.
5. Архитектурные проверки #161 и #173 закрепляют закрытую поверхность decoder/Catalog: они отслеживают транзитный export, прямой re-export, alias, default export и `NamespaceImport`. Проверка не зависит от удалённой междоменной `review-final-lint` и не сводится к текстовым совпадениям исходников.

## Ограничения

- PR #137 остаётся draft: без merge, ready и auto-merge.
- Не менять package metadata, lockfile, зависимости, CI/CD и импортные данные.
- Сохранять seeded RNG, stable IDs и разделение runtime и `data/import/**`.
- Не включать изменения вне задач #152–#176.

## Проверка и завершение #176

- Для каждого изменения выполняются точечные тесты, затем проверяется diff.
- На проверенном удалённом снимке кода `6a2ddb6` выполнены полный `npm run check`, отчёт кластеров, обе проверки `git diff --check`, финальное Standards review, точечное повторное Spec/Standards ревью и GitHub secrets/SAST/OSV/CodeQL.
- PR body обновлён для `6a2ddb6`; closing references указываются только для полностью закрытых задач.
- Draft-статус PR сохраняется; merge, ready и auto-merge не включаются.

## Локальная последующая задача к #152

Локальный тикет `.scratch/pr137-review-fixes/issues/01-isolate-effect-choice-player-state.md` реализован как вертикальный срез в проверенном удалённом снимке кода `6a2ddb6`. `PlayerDecisionView` передаёт стратегии глубокие readonly-снимки `request.player` и кандидатов выбора игрока или карты; стратегия не может изменить живое состояние игрока, его зоны, ресурсы, карты или жетоны, но сохраняет выбор по устойчивой identity, порядок кандидатов и события выбора.

Этот срез усиливает границу стратегии в уже поставленном коде. Он не меняет исходные критерии приёмки #152: канонический выбор Defense по-прежнему определяется до вызова стратегии.

Этот завершающий документационный коммит не меняет проверенный снимок кода `6a2ddb6`; он только фиксирует его доказательства и не создаёт самоссылочный SHA.
